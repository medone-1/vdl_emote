-- Configuration table
Config = {
    OpenKey     = 0x3C0A40F2,   -- Keybind used to open the menu (hex key code)
    OpenCommand = "vdl",        -- Command name used to open the menu
    UseCommand  = false,        -- If true, menu can be opened with a command instead of a key
    CommandName = "emote"       -- The command to trigger emotes (only used if UseCommand = true)
}
